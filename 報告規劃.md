# 報告規劃 — 最終合併簡報（已定案）

> 主題：用 Volatility 還原 RAM Dump 內容
> 對應素材：`計畫書.md`、`SOP.md`、`todo.md`
> 假設聽眾：一般資工背景同學
> **最終結果＝單一合併簡報 `報告_合併_v1.pptx`（共 15 頁）**。前段為課前預習材料（§1~§5），後段為上課報告（§6~§8）。原本的 `第一部份_預習_v1.pptx` / `第二部份_報告_v1.pptx` 與兩支 `build_*.py` 只是中間產物，**以合併版為唯一真相來源**。
>
> ✅ **數據已用實跑 Vol3 的真實輸出回填**。關鍵真值：dump 時間 `2018-08-04 19:34:22`；`Rick And Morty`=PID 3820（父為 explorer 2728）；`vmware-tray.exe`=PID 3720（父為 Rick And Morty 3820，晚 7 秒建立）；`LunarMS`=708→遊戲伺服器 `77.102.199.102:7575`；`BitTorrent`=2836；正版 `vmtoolsd.exe`=1428（掛 services.exe）；`vmware-tray` 無對外連線；md5 `ad51f4ada4151eab76f2dce8dea69868`。
>
> 三個核心指令：`windows.info`（系統資訊）/ `windows.psscan`（掃描還原進程與父子關係）/ `windows.netscan`（網路連線）。

---

## 設計原則（一句話版）

- **前段（§1~§5）是背景+安裝手冊**，不是表演。能查什麼、怎麼裝，放這裡，課前自讀。
- **後段（§6~§8）是表演**：新聞案例帶出動機 → 看影片跑三個指令 → 影片後拆解把紅旗串成攻擊鏈。
- **同一件事不講三遍**：概念在前段 §1~§2，判讀原理只在 §8 影片後拆解講。

---

## 最終簡報逐頁（15 頁）

| 頁 | 章節 | 標題 | 內容重點 / 附圖 |
|---|---|---|---|
| 1 | 封面 | 網路安全期末報告 — 用 Volatility 還原 RAM Dump 內容 | 副標「記憶體鑑識」；樣本 OtterCTF 2018 / 工具 Vol3；組員資訊 |
| 2 | §1（1/2） | 介紹 RAM 鑑識：什麼是記憶體鑑識 | RAM 快照→還原「當時在做什麼」；硬碟 vs 記憶體互補；取得/解析/還原三步驟 |
| 3 | §1（2/2） | 為什麼要查 RAM（硬碟 vs RAM） | RAM 揮發但保留現場；程式要執行就得進 RAM；勒索軟體金鑰仍在 RAM 的例子 |
| 4 | §2（1/2） | RAM 裡面有什麼能查：四類關鍵證據 | 進程 / 網路連線 / 命令列 / 解密後資料；**附圖**：自機 `ps`、`ss` 截圖 |
| 5 | §2（2/2） | 這些資料在記憶體裡怎麼被存 | EPROCESS / _TCP_ENDPOINT・_UDP_ENDPOINT / PEB 各存什麼欄位；Vol3 靠 symbol(PDB) 把 raw bytes 翻成可讀欄位 |
| 6 | §3（1/2） | RAM dump 怎麼取得：三種來源 | ① Live 工具 dump ② VM 快照（本樣本走這條，VMware .vmem）③ Crash dump |
| 7 | §3（2/2） | dump 格式科普 | raw / crash dump / VMware(.vmem)；Vol3 多數可直讀 |
| 8 | §4 | 實作環境 | Linux + Python 3.11 + Vol3（conda）；**附圖**：fastfetch 系統、conda+vol 2.28.0 |
| 9 | §5 | 安裝 Volatility 3 | `vol -h` 驗證；symbol(PDB) 首次自動下載或抓 windows.zip 用 `-s`；**附圖**：vol -h、GitHub symbol 下載 |
| 10 | §6（1/2） | 新聞案例：侵襲臺灣高科技產業的外國 APT（2020） | CyCraft「Operation Skeleton Key」、攻擊組織 Chimera、鎖定台灣半導體；fileless 不落地→須逐台做記憶體鑑識；**附圖**：news.png；附來源網址 |
| 11 | §6（2/2） | 攻擊者擦得掉硬碟，擦不掉記憶體 | 反鑑識手段（清 log/wipe/加密/fileless）；要執行就得載入 RAM 的物理事實 |
| 12 | §7 | 實作影片 | ▶ 看三個指令跑起來；嵌 YouTube 縮圖連結 `https://youtu.be/MObLNDzExd4`；下方列三指令各回答什麼問題 |
| 13 | §8（1/3） | 影片後拆解·命脈：三個指令各解析什麼結構 | 表：info→kernel 核心結構→OS 版本/dump 時間；psscan→掃描比對 EPROCESS pool 標籤→進程+PPID 父子；netscan→_TCP/_UDP_ENDPOINT pool tag→網路連線 |
| 14 | §8（2/3） | 把資訊串成攻擊鏈 | info 定時空 → psscan 進程樹（PPID）→ netscan 連線；結論偽裝程式父進程是盜版下載 |
| 15 | §8（3/3） | 結論（三個 takeaway） | 1 名稱會說謊、父子關係不會 2 路徑與血緣比名字可靠 3 三指令組合才有威力 |

> 結論收束在 §8（3/3），**沒有獨立結論頁**。**Q&A 口頭進行，不放投影片**（準備方向見文末附錄）。

---

## 前段 · 課前預習（§1~§5，slide 2~9）

> **目的**：在進教室前對「記憶體鑑識」建立共同 baseline，並讓想動手的人能照著把環境裝好。
> **載體**：合併簡報前 9 頁；可自讀。

各章重點已列在上方逐頁表。設計取捨：

- **§2 是「能查什麼」的主場**，四類證據講全；後段不再重講。
- **Vol3 框架級原理（symbol/PDB 把 raw bytes 翻成結構化欄位）併在 §2（2/2）講**；逐指令解析哪個結構，留到 §8。
- **§4 單頁純實作環境**（Linux + conda + vol 2.28.0），**§5 單頁安裝 +symbol**（`vol -h` 驗證、windows.zip）。
- 核心結構（EPROCESS / PEB / _TCP_ENDPOINT）**只在 §2 露名**，不展開；展開留到 §8。

---

## 後段 · 上課報告（§6~§8，slide 10~15，約 35~40 分鐘）

> **時間預算**：影片 10 分 + 現場講解 25~30 分。
> **流程**：新聞案例（動機）→ ▶ 影片（看指令跑起來）→ 影片後拆解（命脈，把紅旗串成攻擊鏈）。

### §6｜新聞案例（slide 10~11）

- **採用案例：CyCraft「Operation Skeleton Key」（2020）**，來源 <https://www.cycraft.com/post/apt20200818>。
  - 攻擊組織 **Chimera**，鎖定台灣半導體 / IC 設計與供應鏈，竊取 IC 設計文件、SDK、原始碼等商業機密。
  - **攻擊手段**：PowerShell 把惡意程式直接注入記憶體（fileless）、硬碟不落地 → 磁碟掃不到，得逐台做記憶體鑑識才揪得出來。
- 第二頁收束成原則：**攻擊者擦得掉硬碟，擦不掉記憶體**——再乾淨的硬碟，要執行就得先載入 RAM，現場仍在記憶體裡。

### §7｜▶ 實作影片（slide 12，約 10 分）

簡報頁嵌 YouTube 縮圖（點擊開影片）：`https://youtu.be/MObLNDzExd4`。

**影片腳本**（拍攝順序，對應 `SOP.md`；輸出皆為真跑值）：

| 影片時間軸 | 內容 | 對應 SOP |
|---|---|---|
| 00:00~00:30 | 環境簡介、Vol3 已裝好（不示範安裝） | 安裝段 |
| 00:30~01:00 | 介紹 OtterCTF.vmem，`file` 與 `md5sum`（`ad51f4...`）確認 | 前置 |
| 01:00~02:30 | `windows.info` — 確認 Win7 SP1 x64、dump 時間 `2018-08-04 19:34:22` | 指令 ① |
| 02:30~05:30 | `windows.psscan` — 進程全現身，**圈出 `Rick And Morty`(3820) 與其子進程 `vmware-tray.exe`(3720)**，點出 PPID 關係與「晚 7 秒建立」 | 指令 ② |
| 05:30~08:30 | `windows.netscan` — `LunarMS`(708) 連遊戲伺服器 `77.102.199.102:7575`；**`BitTorrent`(2836) 一票 peer 連線 = 正在下載盜版**；`vmware-tray` 無外連 | 指令 ③ |
| 08:30~10:00 | 進程樹 + 攻擊鏈圖收束 | §8 |

**影片製作建議**：螢幕錄影 + 旁白（旁白負責「指令怎麼跑」，判讀留到 §8）；`psscan` 出來時對 `vmware-tray.exe` 那行加 callout（「⚠️ 父進程是盜版下載？」）；plugin 等待時間加速。

### §8｜影片後拆解·命脈（slide 13~15，約 10 分，命脈）

> 影片只讓人看到指令「跑起來」；**「為什麼 vmware-tray 是惡意的」這個 aha 在這裡才發生**。結構原理也在這裡配合紅旗一起講。

**（1/3）結構原理表**（簡報只列有效欄位）：

| plugin | 解析的結構 | 所以能還原 |
|---|---|---|
| `windows.info` | kernel 核心結構 | OS 版本、dump 時間 |
| `windows.psscan` | 掃描比對 `EPROCESS` 的 pool 標籤 | 所有進程 + PID/PPID 父子關係 |
| `windows.netscan` | `_TCP_ENDPOINT` / `_UDP_ENDPOINT` pool tag | 網路連線（IP / port） |

**（2/3）攻擊鏈**：

```
[info]     ─► 系統：Win7 SP1 x64，dump 時間 2018-08-04 19:34:22
   ▼
[psscan]   ─► explorer.exe(2728)
              ├─ BitTorrent(2836)      19:27:07   下載盜版種子
              ├─ LunarMS(708)          19:27:39   玩線上遊戲
              └─ Rick And Morty(3820)  19:32:55   盜版「Rick and Morty」下載
                   └─ vmware-tray.exe(3720) 19:33:02  假 VMware 元件（晚 7 秒）
              對照：正版 vmtoolsd.exe(1428) 掛在 services.exe 下，路徑、血緣都不同。
   ▼
[netscan]  ─► LunarMS→77.102.199.102:7575（遊戲伺服器）；BitTorrent→一堆 peer（下載）；vmware-tray 無對外連線
   ▼
 ─── 結論：偽裝成 VMware 元件的惡意程式，父進程是盜版下載。
```

> 公開分析另把該惡意程式定性為勒索軟體 HollowCrypt、落地於 `...\Temp\RarSFX0\`——屬**外部 Vol2 writeup 佐證**，非本次三指令輸出。

**（3/3）三個 takeaway**：

1. **進程名稱會說謊，父子關係不會** — 名字像 VMware 元件，父進程卻是盜版下載、還晚它 7 秒出生，就是偽裝。
2. **路徑與血緣比名字可靠** — 正版由 services.exe → vmtoolsd.exe 啟動；冒牌貨掛在一個盜版下載底下。
3. **組合使用才有威力** — info 定時空、psscan 定血緣、netscan 還原行為，三者交叉才拼出完整攻擊。

---

## 附錄 · Q&A 準備（口頭，無投影片）

| 可能問題 | 準備答案方向 |
|---|---|
| 「拿 RAM dump 不是要 admin 嗎？實務上怎麼取得？」 | IR 流程：機器送鑑識實驗室、或現場用工具 dump |
| 「Vol3 跟 Vol2 差在哪？」 | Win7+ 用 Vol3；老 OS（XP）用 Vol2 |
| 「如果攻擊者連 RAM 都加密呢？」 | 執行時仍要解密 → 仍會在 RAM 留痕 |
| 「我可以用這個看自己電腦在跑什麼嗎？」 | 可以，但 Win10/11 需對應 symbol，建議用 winpmem 抓 dump |
| 「Linux 也能 forensic 嗎？」 | 可以，Vol3 有 `linux.*` plugin 系列 |
